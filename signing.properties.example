# Copy to signing.properties (ignored by Git) and fill in your release key details.
# Paths may be absolute or relative to the project root. Use forward slashes.
storeFile=/absolute/path/to/step-mocker-release.jks
storePassword=YOUR_STORE_PASSWORD
keyAlias=step-mocker
keyPassword=YOUR_KEY_PASSWORD
